import { IntegrationsView } from '@/features/integrations/integrations-view';

export const metadata = { title: 'Integrations — NEXUS' };

export default function IntegrationsPage() {
  return <IntegrationsView />;
}
