import { WorkflowBuilder } from '@/features/workflows/workflow-builder';

export const metadata = { title: 'Workflows — NEXUS' };

export default function WorkflowsPage() {
  return <WorkflowBuilder />;
}
