import { NotificationsView } from '@/features/notifications/notifications-view';

export const metadata = { title: 'Notifications — NEXUS' };

export default function NotificationsPage() {
  return <NotificationsView />;
}
